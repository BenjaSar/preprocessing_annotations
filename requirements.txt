# Core dependencies
fitz>=0.0.1
Pillow>=9.0.0
opencv-python>=4.6.0
numpy>=1.21.0

# OCR (PaddleOCR 2.x is the default backend for better accuracy)
paddlepaddle-gpu>=2.6.0,<3.0  # PaddleOCR GPU engine (CUDA 11.8 bundled, ~759MB)
paddleocr>=2.7.0,<3.0          # PaddleOCR wrapper (2.x API, incompatible with 3.x)
easyocr>=1.6.0                 # Optional: fallback backend (use --ocr-backend easyocr to switch)

# VLM (Claude + Qwen2.5-VL)
anthropic>=0.7.0
transformers>=4.36.0  # For Qwen2.5-VL local inference
qwen-vl-utils>=0.0.12 # Qwen2.5-VL utilities
unsloth[cu121]>=2024.1  # Optimized Qwen inference (2x faster, 70% less VRAM)

# Semantic reconciliation
shapely>=2.0.0  # Polygon operations for spatial matching

# Segmentation
segment-anything>=1.0
torch>=2.0.0
torchvision>=0.15.0

# Optional but recommended
tqdm>=4.65.0  # Progress bars
